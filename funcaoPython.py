#funções em python são blocos de código reutilizáveis que realizam uma tarefa específica. 
# Elas ajudam a organizar o código, melhorar a legibilidade e evitar a repetição. 
# Você pode definir suas próprias funções usando a palavra-chave def, seguida pelo nome da função e parênteses.
#Exemplo de função simples
def quadrado(x):
    return x * x    

#vamos usar a função quadrado para calcular o quadrado dos números de 0 a 9
#vou usar outro arqivo para mostrar o uso da função quadrado, pois o arquivo forPython.py tem um exemplo de loop for e não de função.
""" for i in range(10):
    print(f"O quadrado de {i} é {quadrado(i)}") """